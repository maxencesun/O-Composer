# Build provenance

The checked-in artifacts were built on 2026-07-11 from the following fixed
inputs:

| Component | Version / revision |
| --- | --- |
| OpenOrienteering Mapper | `064e6c943ee963277f1e930bda595723acd3e8c6` |
| Qt for WebAssembly | `5.15.2`, official `wasm_32` archive |
| Emscripten | `1.39.8` (`releases-9e60f34accb4627d7358223862a7e74291886ab6-64bit`) |
| PROJ | `7.2.1` |
| SQLite amalgamation | `3.34.1` (`3340100`) |

Mapper source: <https://github.com/OpenOrienteering/mapper/tree/064e6c943ee963277f1e930bda595723acd3e8c6>

The build adds the thin bridge in `source/mapper_wasm_bridge.cpp` and the
marked Emscripten-only CMake changes generated by `source/install-bridge.mjs`.
No OCAD parser behavior is patched: import, validation and export are invoked
directly from Mapper. The complete commands and required environment are in
`source/BUILD.md`.

The distributable was linked with memory growth and browser filesystem
support enabled. `mapper-converter.data` contains the PROJ 7.2.1 data tree,
including `proj.db`, preloaded into Emscripten at `/share/proj`.

The JavaScript compatibility fallback at `src/ocd/ocd2omap.js` is an exact
copy of the validated `ocd2omap-frontend/src/ocd2omap.js` implementation at
integration time. Its SHA-256 is:

```text
70d1078f9f5ca51064f4dbd4b8b24ff7f6e88c86e0c7d461baa224bbd436334b
```
